'use strict';

var update = require('update-contributors');
var extend = require('extend-shallow');

module.exports = function (app, base, env) {
  // var logger = app.logger('update-contributors');

  // app.task('default', function (cb) {
  //   try {
  //     var pkg = base.getFile('package.json');
  //     var data = extend({}, pkg.json);
  //     // logger('done');
  //     update(data, function (err, results) {
  //       if (err) return cb(err);
  //     console.log(results)
  //       pkg.json = results;
  //       cb();
  //     });
  //   } catch (err) {
  //     cb(err);
  //   }
  // });
};
